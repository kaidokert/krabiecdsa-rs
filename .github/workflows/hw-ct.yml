name: hw-ct

on:
  push:
    branches:
      - main
      - 'feature/**'
  pull_request:
  workflow_dispatch:

jobs:
  hardware:
    uses: kaidokert/krabi-caliper-rs/.github/workflows/hw-ct.yml@hw-ct-v1
    with:
      invocation-name: krabiecdsa
      campaigns-json: '["krabiecdsa-ct-jtrace-f407-30mhz","krabiecdsa-smoke-jtrace-f407-168mhz"]'
      working-directory: ct-verify/cyccnt-hardware
      prepared: true
