name: hw-ct

on:
  push:
    branches:
      - main
      - 'feature/**'
  pull_request:
  workflow_dispatch:

jobs:
  hardware:
    uses: ./.github/workflows/reusable-hw-ct.yml
    with:
      campaigns-json: '["krabiecdsa-ct-jtrace-f407-30mhz","krabiecdsa-smoke-jtrace-f407-168mhz"]'
      working-directory: ct-verify/cyccnt-hardware
      prepared: true
